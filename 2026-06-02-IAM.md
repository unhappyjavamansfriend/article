配置服務帳戶（Service Account）」非常關鍵，它是用來**授權給未來的 Cloud Run，讓它有合法的鑰匙可以打開這個 Bucket 讀寫資料**。

為了保持最優雅、最安全的雲端架構（Principle of Least Privilege），我們**強烈建議單獨建立一個專屬的服務帳戶**，而不是使用權限過大的預設帳戶。

以下是手把手在 GCP Console 設定的完整步驟：

# 第一步：建立一個專屬的 Memos 服務帳戶

1. 點擊 GCP 左上角的 **導覽功能表 (三條線 ☰)**。
    
2. 導航到 **IAM 與系統管理 (IAM & Admin) ➔ 服務帳戶 (Service Accounts)**。
    
3. 點擊上方的 **「+ 建立服務帳戶 (CREATE SERVICE ACCOUNT)」**。
    
4. **填入基本資料**：
    
    - **服務帳戶名稱**：輸入 `memos-runner`。
        
    - **服務帳戶 ID**：會自動生成（例如 `memos-runner@你的專案ID.iam.gserviceaccount.com`）。
        
    - **說明**：寫一下 `用於 Cloud Run 執行 Memos 並讀寫 GCS`，方便以後辨識。
        
5. 點擊 **「建立並繼續 (CREATE AND CONTINUE)」**。
    
6. **設定專案權限（可跳過，我們稍後在儲存桶單獨設定）**：
    
    - 這裡先不用給它全專案的權限，直接點擊 **「繼續 (CONTINUE)」**，然後點擊 **「完成 (DONE)」**。
        

# 第二步：把儲存桶的「鑰匙」單獨發給這個服務帳戶

我們要限定這個服務帳戶「只能讀寫剛剛建好的那個 Memos Bucket」，其他桶子一概不給碰，這在工程上是最安全的作法。

1. 複製你剛剛建好的服務帳戶 Email（就是 `memos-runner@...` 那串）。
    
2. 回到 **Cloud Storage ➔ 儲存桶 (Buckets)**，點擊你剛建好的那個 Memos 儲存桶名稱。
    
3. 切換到上方的 **「權限 (PERMISSIONS)」** 分頁。
    
4. 往下拉一點，點擊 **「授予存取權 (GRANT ACCESS)」** 按鈕。
    
5. **新增主體 (New principals)**：把剛剛複製的服務帳戶 Email 貼上去。
    
6. **選取角色 (Select a role)**：
    
    - 點開下拉選單，搜尋或選擇 **「Cloud Storage」➔「儲存空間物件管理員 (Storage Object Admin)」**。
        
    - _(這個角色具備了完整的讀取、寫入、覆寫、刪除該儲存桶內物件的權限，非常適合需要控制 SQLite 資料庫的 Memos)_。
        
7. 點擊 **「儲存 (SAVE)」**。