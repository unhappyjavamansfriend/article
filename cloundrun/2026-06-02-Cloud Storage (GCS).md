# 儲存空間級別（Storage Class

****Cloud Storage 最關鍵的**省錢核心機制**。

Google Cloud 的邏輯很簡單：**你越少去讀取的檔案，存放在冷資料區的「儲存費」就越便宜，但是「讀取費（擷取費）」就越貴。**

為了讓你部署 Memos 或是未來的專案時能選到最划算的方案，以下幫你拆解這四個級別的真正用途：

## 四大儲存空間級別拆解

### 1. Standard (標準) **自建 Memos 推薦選這個**

- **用途**：適合存放**經常被讀取、修改**的資料。
    
- **特性**：儲存單價最高，但**讀取、擷取資料完全免費（免作業費）**，且沒有最低儲存天數限制。
    
- **為什麼 Memos 選這個？**：因為 Memos 的 SQLite 資料庫（`db` 檔案）非常小（可能幾 MB 到幾百 MB），佔用的空間微乎其微，但你寫日記、看動態時，Cloud Run 會**瘋狂、頻繁地去讀寫這個檔案**。選 Standard 可以讓你免去高昂的讀寫作業費。
    

### 2. Nearline (低頻存取)

- **用途**：適合**每個月頂多讀取一次**的資料。例如：每個月底的財務報表、系統日誌（Logs）。
    
- **限制**：資料最少要存滿 **30 天**，提早刪除會被收滿 30 天的錢。讀取時會收取少許的擷取費。
    

### 3. Coldline (冷存取)

- **用途**：適合**每季（三個月）或半年才看一次**的資料。例如：每年的報稅資料、舊專案的封存檔。
    
- **限制**：資料最少要存滿 **90 天**。儲存費非常便宜，但讀取（擷取）費用變高。
    

### 4. Archive (封存)

- **用途**：適合幾年才讀取一次，甚至純粹為了法規合規「拜託永遠不要用到」的災難備份。
    
- **限制**：資料最少要存滿 **365 天**。儲存費便宜到可以忽略，但一旦你要把資料拔出來，會被收取極其高昂的擷取手續費。

## 神功能：Autoclass (自動化管理)

畫面上應該有另外一個選項叫做 **Autoclass**。

- **它是做什麼的？**：如果你勾選它，Google 會用 AI 幫你監控這個 Bucket 裡面的每個檔案。如果某張照片你放了半年都沒人看，Google 會**自動把它降級**到 Coldline 幫你省錢；哪天你突然點開那張照片，它又會**自動升回** Standard。
    
- **缺點**：啟用 Autoclass 需要額外付一筆小額的「管理費」。
    
- **建議**：對於 Memos 這類個人小專案，**不需要開啟 Autoclass**，手動固定在 **Standard** 就最省錢了。

## 「省錢」做到極致，未來的標準打法是**開兩個不同級別的 Bucket**：

1. **`memos-live-bucket` (設定為 Standard)**： 專門用來掛載給 Cloud Run，存放你的 `memos_prod.db` 主資料庫。因為天天要讀寫，Standard 最划算。
    
2. **`memos-backup-bucket` (設定為 Nearline 或 Coldline)**： 你可以寫一個簡單的腳本，每週把 db 檔案打包壓縮，丟進這個備份儲存桶。因為只是防萬一的備份，很少去讀取，放冷存取區每個月可能只要幾塊錢台幣。
# 如何控制物件的存取權

攸關**資料安全**與**荷包防禦**的關鍵設定。

結論是：**強烈建議直接勾選「強制禁止公開存取（Enforce public access prevention）」。**

在自建個人專案（尤其是 Memos 日記或記帳工具）的場景下，把這個選項勾起來有兩個最重要的工程原因：

### 1. 它是你日記與資料庫的「防彈衣」

你的 Memos 資料庫（`memos_prod.db`）裡面存著你所有的私密日記、甚至未來可能包含一些敏感的個人足跡。

- **如果沒開啟**：萬一哪天你或團隊成員在設定權限（ACL 或 IAM）時不小心手滑，把某個檔案設成了「公開可讀（allUsers）」，那全世界的人只要猜到網址，就能直接下載你的整個資料庫檔案。
    
- **開啟的好處**：這是一道**系統級的強制閘門（Guardrail）**。一旦勾選，不論你未來的 IAM 權限怎麼亂設，GCP 會直接從底層封死，任何人想把這個儲存桶裡的物件變成 Public 網址都會直接被系統攔截拒絕。
    

### 2. 保護你的錢包，防止被「刷流量（DDOS）」

這在雲端開發上是非常經典的「鬼故事」。

- 如果你的儲存桶允許公開存取，而你又在裡面放了日記的圖片或備份檔，一旦網址外流，或是被惡意爬蟲盯上，對方可以寫個迴圈一秒鐘對你的圖片發起幾萬次下載請求。
    
- Cloud Storage 的網路流出流量（Egress）是需要計費的。被惡意刷流量可能會讓你月底收到一筆驚人的帳單。
    
- **勾選禁止公開存取**後，只有通過 GCP 身份驗證的服務（例如我們接下來要架設、帶有專屬服務帳戶的 **Cloud Run**）才有資格伸手進去拿資料，直接免疫了網際網路上的惡意流量攻擊。
    

## 常見疑問：那我的 Memos 網頁還能公開給別人看嗎？

很多人會擔心：「如果我禁止了 Bucket 的公開存取，那我在 Memos 裡發布的『公開（Public）動態』，其他人是不是就看不到了？」

**完全不用擔心，一樣看得到！** 它的運作邏輯是這樣的：

1. **路人（網際網路）** ➔ 連入你的 **Cloud Run 網址**。
    
2. **Cloud Run（後端程式）** ➔ 擁有安全憑證，合法的從 **Cloud Storage (Bucket)** 讀取資料。
    
3. **Cloud Run** ➔ 把資料處理好、渲染成漂亮的網頁，再傳回給 **路人**。
    

也就是說，外界的人是透過 Memos 這個「守門員」來間接讀取資料，而不是直接去撈你的 Bucket 桶子。
# 存取控管（Access Control

**想用哪種方式來發放這顆硬碟的鑰匙？」**

結論直接給你：**強烈建議毫不猶豫選擇「統一 (Uniform)」！**

這不只是個人自建 Memos 的推薦設定，也是現在雲端開發（Cloud Native）最主流、最安全的標準實踐。

## 為什麼選「統一 (Uniform)」？

「統一」代表**全權交給 IAM（身分權限管理系統）負責**。

- **白話解釋**：這就像是一棟大樓（Bucket），你只在**大門口**設一個警衛（IAM 規則）。警衛手上有一份名單，寫著誰（例如你的 Cloud Run 服務帳戶）可以進來讀寫。只要名單上有，大樓裡的任何房間（檔案/物件）他都可以進去。
    
- **優點**：權限非常乾淨。你只要在專案層級確保你的 Cloud Run 有權限讀寫這個 Bucket，一切就搞定了。不會有任何「靈異現象」或意外的權限漏洞，管理起來最輕鬆。
    

## 為什麼不要選「精細 (Fine-grained)」？

「精細」代表除了大門警衛（IAM）之外，你還啟用了舊時代的 **ACL（存取控制清單）**。

- **白話解釋**：這代表大門警衛放人進來之後，大樓裡的**每一個單獨的檔案（每一張圖片、每一個 db 檔案）都可以自己帶一把獨立的鎖**。
    
- **缺點**：
    
    1. **管理地獄**：萬一未來你的 Memos 上傳了幾千張照片，其中有幾張照片的 ACL 權限不知道為什麼卡住或設錯，就會導致你在 Memos 網頁上突然看不到某幾天的照片。去排查這種「物件等級」的權限問題會讓人極度抓狂。
        
    2. **安全隱憂**：ACL 非常容易因為程式碼沒寫好、或是手滑下錯指令，導致某些不該公開的檔案被單獨設成公開。
        

## 💡 提示：畫面提到的「90 天限制」

畫面上寫著「90 天過後，這個選項就永久無法變更」，這其實是 Google 為了防止開發者架構定型後亂改導致生產事故的保護機制。

不過別擔心，因為我們的目標非常明確：**我們就是要讓 Cloud Run 去讀寫一整個完整的資料夾，沒有要單獨去閹割或開放某一個特定 SQLite 檔案的權限**。因此直接鎖定「統一 (Uniform)」是最符合微服務架構的作法。
# 保護物件資料的方式

來到建立 Bucket 的最後一步「保護物件資料的方式」，這裡 Google 是在問你：**「如果檔案不小心被刪除、覆寫，或者遇到意外時，你需要多少『反悔』的機會？」**

對於自建 Memos 日記專案，這裡有兩個直接影響你**荷包成本**與**資料安全**的關鍵設定，建議這樣配置：

## 最具成本效益的配置建議

### 1. 虛刪除政策（Soft Delete）：建議設為「0 天」（停用）

- **這是什麼**：虛刪除（Soft Delete）是 GCP 預設開啟的功能。如果開啟（預設 7 天），當 Memos 在後台寫入、覆寫 SQLite 資料庫時，舊版本的檔案不會立刻消失，而是會在後台變成「隱形狀態」幫你保留 7 天，以防你手滑刪錯。
    
- **為什麼建議關閉（設為 0 天）？**：因為 Memos 的資料庫（`memos_prod.db`）是一個**會頻繁被覆寫、頻繁更新的單一檔案**。如果開啟虛刪除，每一次你新增一則 Memo，GCP 後台就會把舊的 db 檔案存一份起來留 7 天。這樣會在不知不覺中**累積大量的隱形垃圾檔案，導致你的儲存空間暴增，被收取冤枉的儲存費**。
    
- **操作**：勾選 **「設定自訂保留時長」** ➔ 將天數改為 **`0`**（這會直接停用虛刪除功能，檔案刪除就永久移除）。
    

### 2. 物件版本管理（Object Versioning）：建議「不要勾選」（維持關閉）

- **這是什麼**：這就像是 Google Drive 的「版本歷程紀錄」。每當你修改日記，它就會把舊版 db 留下來，讓你以後可以「坐時光機」回到某一天的資料庫狀態。
    
- **為什麼建議關閉？**：原因跟上面一樣。SQLite 資料庫天天都在變動，開啟版本管理會像細胞分裂一樣在你的儲存桶裡產生無數個歷史版本，這會讓你的**帳單費用直線上升**。
    
- **更優雅的備份打法**：身為工程師，我們通常會另外寫一個簡單的 Cron job 或腳本，每週或每天深夜把 `memos_prod.db` 單獨備份一份改名為 `memos_backup_20260602.db` 存到另一個地方，而不是交給 GCP 進行無腦的全版本留存。
    

### 3. 保留（Retention）：建議「不要勾選」（維持關閉）

- **這是什麼**：這是企業用來應付法規（例如金融稽核）的 WORM（Write Once, Read Many）機制。一旦設定「所有檔案建立後 1 年內不准刪除」，**連你這個最高管理員、甚至是 Google 客服都絕對無法刪除它**。
    
- **為什麼建議關閉？**：如果你不小心勾了這個並設定了時間，萬一哪天你不想玩 Memos 了想清空 Bucket，GCP 會直接拒絕你，強迫你必須付費把這個桶子養到保留期限到期為止。