其實，MariaDB 之所以不叫 **MySQL 2**，背後不是因為技術問題，而是因為一場**商標版權的大戰**，以及創始人對自己**兩個女兒的愛**。

以下用 3 個核心原因，為你揭開這段有趣的開源歷史：

## 1. 最殘酷的現實：商標權在對手手上

「MySQL」這個名字是有**商標權保護**的。

MySQL 最初是由瑞典公司 MySQL AB 開發。後來這家公司在 2008 年被 Sun（昇陽電腦）收購，接著在 2010 年，Sun 又被大魔王 **Oracle（甲骨文）** 收購了。

這意味著，**`MySQL` 這五個字母的商標產權，現在完全屬於 Oracle。**

當 MySQL 的親生父親——**麥克爾·維德紐斯（Michael "Monty" Widenius）** 因為不滿 Oracle 的商業壟斷作風，決定出走並把 MySQL 的原始碼複製一份（Fork）出來做新專案時，**他依法絕對不能在自己的新產品名字裡用到「MySQL」這五個字**，否則會被 Oracle 的法務部門告到破產。所以，他根本不可能叫它 MySQL 2。

## 2. 浪漫的原因：創始人的女兒們

既然不能叫 MySQL，那為什麼要叫 **MariaDB** 呢？這就要講到創始人 Monty 的浪漫命名堅持了：

- **My**SQL：其實是以 Monty 的**大女兒 "My"（讀音類似「米」）** 來命名的。
    
- **Maria**DB：當他出走建立新資料庫時，他就決定用他的**小女兒 "Maria"** 的名字來命名。
    

所以這兩個資料庫在本質上，就是**姊妹關係**！ _(順帶一提，Monty 還做過另一個開源儲存引擎專案叫 **MaxDB**，那是用他**兒子 Max** 的名字命名的。只能說這爸爸真的非常公平，所有小孩都有專屬的資料庫。)_

## 3. 「MySQL 2」這個名字其實早就被工程界拿走了

在軟體工程的世界裡，通常「`xxx2`」這種名字會被拿來當作程式語言的驅動套件（Driver / Library）名稱，而不是資料庫本體。

事實上，如果你寫過 Node.js (JavaScript) 或 Ruby，你在連接 MySQL 時，一定會用到一個非常知名的 npm/gem 套件就叫做 **`mysql2`**。

- `mysql` 套件：第一代舊版驅動。
    
- `mysql2` 套件：第二代優化版、支援非同步（Async）的現代驅動。
    

為了避免全球開發者在安裝套件與安裝資料庫時產生嚴重的精神錯亂，MariaDB 採用一個完全無關、但又帶有血緣暗示的新名字，是最高明的做法。

## 既然名字完全不同，為什麼大家都說它是「真．MySQL」？

因為 MariaDB 在創立之初，設定了極度瘋狂的 **「100% 掉包相容（Drop-in Replacement）」** 目標：

- **核心指令完全一模一樣**：在 MariaDB 裡，底層的執行檔依然叫 `mysqld`，指令介面依然是打 `mysql -u root -p`。
    
- **連接埠（Port）相同**：同樣預設走 `3306`。
    
- **資料檔案直接通用**：在早期的版本中，你把 MySQL 的資料夾直接複製貼給 MariaDB，它甚至能直接開機讀取，完全不需要匯出匯入。
    

所以，雖然它迫於法律現實和家庭和諧（？）改名叫 **MariaDB**，但因為它保留了最純正的開源血統與靈魂，在開源社群眼中，它才是那個沒有被商業利益污染的 **「真．MySQL」**！